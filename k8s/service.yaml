apiVersion: v1
kind: Service
metadata:
  name: mail-service-service
  namespace: ase-12
spec:
  selector:
    app.kubernetes.io/name: mail-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
