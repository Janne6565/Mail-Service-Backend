apiVersion: v1
kind: Service
metadata:
  name: mail-service
  labels:
    app.kubernetes.io/name: mail-service
spec:
  selector:
    app.kubernetes.io/name: mail-service
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: management
      port: 8081
      targetPort: management
